node_modules/
dist/
dist-test/
*.log
.DS_Store
npm-debug.log*

# mcp-publisher CLI binary (downloaded locally, not committed)
mcp-publisher

# Personal watchlist export — kept out of the public repo (the server reads the
# Apple Stocks app directly and does not need this file).
watchlist.txt

# Internal reverse-engineering notes — not published.
HOW_TO_GET_STOCKS.txt
